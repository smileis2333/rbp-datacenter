spring:
  application:
    name: datacenter-server
  profiles:
    active: test
  cloud:
    nacos:
      config:
        server-addr: 114.132.41.62:8848
        file-extension: yml
        group: DEFAULT_GROUP
